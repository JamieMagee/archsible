---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: instance
    image: base/archlinux
provisioner:
  name: ansible
  lint:
    name: ansible-lint
  env:
    ANSIBLE_LIBRARY: ../../../../library/ansible-aur
scenario:
  name: default
verifier:
  name: goss
  lint:
    name: flake8
    enabled: false
