---
sudo: required

language: python
python:
  - "2.7"

services:
  - docker

install:
  - pip install pipenv
  - pipenv install --dev --system --skip-lock

before_script: chmod +x test-docker.sh

script:
  - yamllint .
  - ./test-docker.sh
